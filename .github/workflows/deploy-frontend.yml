name: deploy-frontend

on:
  push:
    tags: ["v*"]
    paths:
      - "frontend/**"
  workflow_dispatch:
    inputs:
      phase:
        description: "Phase(beta, prod)"
        required: true
      ref:
        description: "Git ref (tag/branch/sha)"
        required: true

concurrency:
  group: deploy-frontend-${{ inputs.phase }}
  cancel-in-progress: false

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.ref || github.ref }}

      - name: Credential
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_ACCESS_KEY_SECRET }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Prepare
        id: prepare
        env:
          DEPLOY_MAP: ${{ vars.DEPLOY_MAP }}
        run: |
          set -euo pipefail

          if [ "${{ github.event_name }}" = "push" ]; then
            TAG="${GITHUB_REF##*/}"
            if [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+b[0-9]*$ ]]; then
              PHASE="beta"
            elif [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
              PHASE="prod"
            else
              echo "Unrecognized tag format: $TAG"
              exit 1
            fi
          else
            PHASE="${{ inputs.phase }}"
          fi

          DOMAIN=$(echo "$DEPLOY_MAP" | jq -er ".${PHASE}.domain")
          S3_BUCKET_NAME=$(echo "$DEPLOY_MAP" | jq -er ".${PHASE}.bucket")
          CLOUDFRONT_DISTRIBUTION_ID=$(echo "$DEPLOY_MAP" | jq -er ".${PHASE}.cloudfront")

          {
            echo "PHASE=$PHASE"
            echo "DOMAIN=$DOMAIN"
            echo "S3_BUCKET_NAME=$S3_BUCKET_NAME"
            echo "CLOUDFRONT_DISTRIBUTION_ID=$CLOUDFRONT_DISTRIBUTION_ID"
          } >> "$GITHUB_OUTPUT"

      - name: Validate
        id: validate
        env:
          DOMAIN: ${{ steps.prepare.outputs.DOMAIN }}
          S3_BUCKET_NAME: ${{ steps.prepare.outputs.S3_BUCKET_NAME }}
          CLOUDFRONT_DISTRIBUTION_ID: ${{ steps.prepare.outputs.CLOUDFRONT_DISTRIBUTION_ID }}
          DISCORD_CLIENT_ID: ${{ vars.DISCORD_CLIENT_ID }}
          GUILD_INVITE_URL: ${{ vars.GUILD_INVITE_URL }}
        run: |
          set -euo pipefail

          MISSING=0
          for V in DISCORD_CLIENT_ID GUILD_INVITE_URL; do
            if [ -z "${!V}" ]; then
              echo "Missing required variable: $V"
              MISSING=1
            fi
          done
          if [ "$MISSING" -ne 0 ]; then
            exit 1
          fi

          {
            echo "PHASE=${{ steps.prepare.outputs.PHASE }}"
            echo "VITE_API_ORIGIN=https://${{ steps.prepare.outputs.DOMAIN }}"
            echo "VITE_DISCORD_CLIENT_ID=$DISCORD_CLIENT_ID"
            echo "VITE_GUILD_INVITE_URL=$GUILD_INVITE_URL"
            echo "S3_BUCKET_NAME=${{ steps.prepare.outputs.S3_BUCKET_NAME }}"
            echo "CLOUDFRONT_DISTRIBUTION_ID=${{ steps.prepare.outputs.CLOUDFRONT_DISTRIBUTION_ID }}"
          } >> "$GITHUB_OUTPUT"

      - name: Run
        working-directory: frontend
        env:
          VITE_API_ORIGIN: ${{ steps.validate.outputs.VITE_API_ORIGIN }}
          VITE_DISCORD_CLIENT_ID: ${{ steps.validate.outputs.VITE_DISCORD_CLIENT_ID }}
          VITE_GUILD_INVITE_URL: ${{ steps.validate.outputs.VITE_GUILD_INVITE_URL }}
          S3_BUCKET_NAME: ${{ steps.validate.outputs.S3_BUCKET_NAME }}
          CLOUDFRONT_DISTRIBUTION_ID: ${{ steps.validate.outputs.CLOUDFRONT_DISTRIBUTION_ID }}
        run: |
          set -euo pipefail
          npm ci
          npm run build
          cd ..
          aws s3 sync frontend/dist s3://$S3_BUCKET_NAME --delete
          aws cloudfront create-invalidation --distribution-id "$CLOUDFRONT_DISTRIBUTION_ID" --paths "/*"
